rule:
  id: drawings.box_text_align_v1.cc9e1863
  name: Box dengan text alignment
  version: 1
  priority: medium
  compatibility:
    pine:
      min: 5
      max: 6
  triggers:
  - type: analyzer
    ast_patterns:
    - node_type: pattern_match
      context: DRAWINGS
      signature: cc9e1863
  preconditions: {}
  parameters: []
  action:
    operation: inject_after
    anchor: TODO
    language: pine
    template: "box.new(bar_index, level, bar_index + 1, level + Range, text = getText,\
      \ \n                                                            text_halign\
      \   = text.align_left, \n                                                  \
      \          text_valign   = text.align_center, \n                           \
      \                                 border_color  = color(na)"
    safety:
      reversible: true
      backup_required: true
      modifies_existing_logic: false
  verification:
    compiler:
      must_pass: true
  fallbacks: []
  dependencies: []
  signature: cc9e1863
  source:
    extractor_version: v2.1
    detector_id: box_text_align_v1
    timestamp: null
