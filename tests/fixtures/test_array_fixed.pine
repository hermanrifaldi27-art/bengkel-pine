//@version=6
indicator("Test Array")
var myArray = array.new<float>()
for i = 1 to 150
    array.push(myArray, close)
plot(array.size(myArray))
while array.size(myArray) > 100
    array.shift(myArray)