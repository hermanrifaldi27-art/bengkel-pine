rules:
- id: objects.obj_in_if_v1.aee27dfd
  name: table.new di dalam if global -> gunakan var untuk deklarasi di luar
  version: 1
  priority: medium
  compatibility:
    pine:
      min: 5
      max: 6
  triggers:
  - type: analyzer
    ast_patterns:
    - node_type: pattern_match
      context: OBJECTS
      signature: aee27dfd
  preconditions: {}
  parameters: []
  action:
    operation: inject_after
    anchor: line 276:9
    language: pine
    template: "var new_var = new.new(...)\nif condition\n    new_var := new.new(...)"
  safety:
    reversible: true
    backup_required: true
    modifies_existing_logic: false
  verification:
    compiler:
      must_pass: true
  fallbacks: []
  dependencies: []
  signature: aee27dfd
  source:
    extractor_version: v2.1
    detector_id: obj_in_if_v1
