//@version=6
indicator("Test Matrix")

var matrix<float> m = matrix.new<float>(0, 9)
var memoryDepth = 500

if barstate.isconfirmed
    matrix.add_row(m, array.from(1,2,3,4,5,6,7,8,9))
while array.size(m) > 500
    array.shift(m)